# 프로젝트 컨텍스트 — CodeTest Feedback Platform

이 문서는 Claude Code가 이 저장소에서 작업할 때 참고하는 컨텍스트입니다. 작업 전 반드시 읽고, 특히 "지금 진행 가능한 작업"과 "Claude Code 작업 시 유의사항"을 확인하세요.

## 프로젝트 개요

IBM Project CodeNet 데이터셋에서 **풀이 기록(제출 이력)이 확보된 AtCoder 문제만** 골라 출제하는 자체 코딩테스트 연습 플랫폼입니다.

핵심 가치: 사용자가 문제를 푸는 동안의 행동(편집 패턴, 재시도 간격, 정지 시간, 붙여넣기 여부 등)을 실시간으로 수집하고, 동일 문제를 푼 다른 응시자(CodeNet 데이터 기반) 대비 통계와, 로컬 LLM이 생성하는 "결과가 아닌 과정" 기반 피드백을 제공합니다.

## 팀 구성과 의존관계

- **팀원A**: CodeNet 데이터 전처리 → Claude(강한 모델)에 투입해 "풀이 과정 및 오류 수정 패턴"을 구조화 → 그 결과를 로컬 LLM(Qwen)용 피드백 생성 프롬프트로 설계
- **팀원B(이 저장소 작업자)**: 플랫폼 뼈대(프론트엔드/백엔드/인프라) 전체 구현

**중요한 순서 의존성**: 백엔드-프론트엔드 API 명세와 DB 스키마는 팀원A의 "풀이 과정 패턴 구조화" 산출물이 나온 뒤 확정합니다. 그 전까지 Claude Code는 아래 "지금 진행 가능한 작업"만 진행하고, 스키마가 필요한 부분은 placeholder로 남겨야 합니다.

## 현재 상태 (2026-07-24 기준)

| 항목 | 상태 |
|---|---|
| 문제 소스: CodeNet의 AtCoder 문제, 풀이 기록 있는 것만 선별 | 확정 |
| 코드 에디터: Monaco Editor (자체 통합) | 확정 |
| 실시간 로그 전송 계층 (`activity-logger.ts`, `useMonacoActivityLogger.ts`) | 구현 완료 (전송 방식만. 수집 항목명은 임시) |
| 로컬 LLM: Ollama + Qwen 계열 | 팀원A가 프롬프트 설계 진행 중 |
| 수집 항목의 정확한 필드/이벤트 목록 | 미확정 — 팀원A 산출물 대기 |
| 백엔드-프론트엔드 API 명세, DB 스키마 | 미착수 — 수집 항목 확정 후 시작 |
| 코드 실행/채점 엔진 | 미확정 — 아래 참고 |

## 중요 미해결 항목 (팀 논의 필요, Claude Code가 임의로 확정하면 안 됨)

1. **코드 실행/채점 엔진**: 사용자가 제출한 코드를 실제로 실행해 CodeNet 테스트케이스와 비교 채점해야 하는데, 기존 계획(크롬 익스텐션 시절)에는 이 컴포넌트가 없었습니다. **Judge0**(오픈소스, REST API, 60개 이상 언어, Docker 기반 샌드박스)를 1순위 후보로 검토 중입니다. 단, Judge0는 과거 샌드박스 이스케이프 취약점(CVE-2024-29021 등)이 보고된 바 있어, 자체 호스팅 시 최신 버전 사용과 네트워크 격리를 반드시 확인하거나 초기엔 호스팅형(클라우드) 티어 사용을 검토해야 합니다. **사용자 코드를 백엔드에서 직접 eval/exec 하는 방식은 절대 금지.**
2. **CodeNet 데이터셋 라이선스**: 문제 지문을 CodeNet에서 그대로 가져와 서비스에 노출해도 되는지(AtCoder 원 저작권 vs CodeNet 재배포 라이선스 조건)를 법무 검토 전까지 확정하지 않습니다. 검토 전에는 문제 지문 대신 AtCoder 원문 링크로 대체하는 방식도 함께 고려.

## 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 코드 에디터 | Monaco Editor | `onDidChangeModelContent` / `onDidPaste` / `onDidFocusEditorText` 등 공식 API 사용, 리버스엔지니어링 불필요 |
| 실시간 로그 전송 | WebSocket + sendBeacon 폴백 | `frontend/lib/activity-logger.ts` |
| 코드 실행/채점 | Judge0 (검토 중, 미확정) | 위 "중요 미해결 항목" 참고 |
| 로컬 LLM 서빙 | Ollama + Qwen | 팀원A 담당. 프롬프트 내용 확정 전까지 mock 응답으로 통신 레이어만 먼저 검증 |
| DB | PostgreSQL (가정, 미확정) | 스키마는 팀원A 산출물 이후 확정 |

## 디렉토리 구조 (제안)

```
/frontend
  /src
    /components/Editor        # Monaco 래핑 컴포넌트
    /lib/activity-logger.ts   # 구현 완료
    /hooks/useMonacoActivityLogger.ts  # 구현 완료
    /pages (or /routes)
/backend
  /src
    /api        # REST/WebSocket 핸들러
    /judge      # Judge0 클라이언트 래퍼
    /llm        # Ollama/Qwen 클라이언트 래퍼 (요청/응답 형태만, 프롬프트 내용은 팀원A 산출물 대기)
    /db         # 스키마/마이그레이션 — 대부분 placeholder, 아래 참고
/infra
  docker-compose.yml  # Judge0, Ollama, Postgres, backend, frontend
```

## 지금 진행 가능한 작업 (수집 항목/스키마 확정과 무관 — 우선순위 순)

| 작업 | 왜 지금 가능한가 |
|---|---|
| 레포 스캐폴딩 + CI 설정 | 스키마와 무관 |
| 인증(회원가입/로그인) | `users` 테이블은 다른 스키마와 독립적 |
| 문제 카탈로그 UI (목록/상세) | CodeNet에서 문제 메타데이터(지문, 제약조건, 예제 입출력)만 우선 추출 — "풀이 과정 패턴"과는 별개 데이터라 팀원A 작업을 안 기다려도 됨 |
| Monaco 에디터 통합 + activity-logger 연결 | 이미 구현된 `activity-logger.ts`/`useMonacoActivityLogger.ts` 그대로 사용, 이벤트 타입 이름만 나중에 맞추면 됨 |
| Judge0 연동 PoC (코드 제출 → 실행 → 결과 반환) | 스키마와 무관, 오히려 최우선 검증 필요 (미해결 항목 1번) |
| WebSocket 서버 스켈레톤 (이벤트 수신 → 우선 콘솔 로그만) | 실제 저장 스키마 없이도 연결 자체는 검증 가능 |
| Ollama 서빙 인프라 (컨테이너, health check API 래퍼) | 프롬프트 "내용"은 팀원A 담당이지만, 요청을 보내고 응답을 받는 통신 레이어는 지금 만들어도 됨 — mock 프롬프트로 우선 테스트 |
| DB 중 확정 가능한 테이블만 먼저 마이그레이션 (`users`, `problems`, `submissions` 기본형) | `events`/`feature_summary` 관련 컬럼은 JSONB로 열어두고 `-- TODO: 팀원A 산출물 확정 후 재설계` 주석 명시 |

## 코딩 컨벤션

- 프론트엔드: TypeScript + React, 함수형 컴포넌트/훅 사용
- 백엔드: (팀 확정 필요 — 언어/프레임워크 TBD, 결정되는 대로 이 문서 갱신)
- 커밋 메시지: 무엇을/왜 변경했는지 한국어 또는 영어로 간결하게

## Claude Code에게: 작업 시 유의사항

- 팀원A가 담당하는 "CodeNet 전처리 → 프롬프트 설계" 영역은 건드리지 않습니다 (별도로 진행 중).
- DB 스키마와 API 명세는 확정하지 말고 placeholder로만 만들며, `-- TODO: 팀원A 산출물 확정 후 재설계` 주석을 반드시 남깁니다.
- 사용자 코드 실행은 반드시 Judge0(또는 동급 샌드박스) 경유. 백엔드에서 직접 eval/exec 금지.
- 수집 로그에는 사용자가 작성한 코드 전문이 포함될 수 있으므로, 저장/전송 시 로그인 정보(이메일 등 개인정보)와 분리 보관.
- 새로운 기술 선택(프레임워크, 라이브러리, 인프라)은 이 문서의 "미확정" 항목에 걸쳐 있다면 임의로 확정하지 말고, 이 문서에 "검토 후보"로 남긴 뒤 팀 확인을 받습니다.
